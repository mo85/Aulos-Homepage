<%= form_for(@concert, :url => url, :html => { :class => "default" }) do |f| %>
  <% if @concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% @concert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<%= render "events/form_fields", :f => f, :event => @concert, :omit_end => true %>

  <div class="actions">
    <%= f.submit label %>
  </div>
<% end %>
