<% if target.size > 0 %>
	<table class="default">
		<tr>
			<th width="70%">Name</th>
			<th width="30%">Aktion</th>
		</tr>
	
		<% target.each do |m| %>
			<tr>
				<td><%= m.person.last_first %></td>
				<td>
				<%= link_to image_tag("icons/pencil.png", :title => "Anpassen..."), edit_membership_path(m) %>
				<%= link_to image_tag("icons/cross.png", :title => "Mitglied entfernen?"), remove_member_project_path(m.project, :person_id => m.person.id), :method => :delete %></td>
			</tr>
		<% end %>
	
	</table>

<% else %>
	<p>
		Für diese Instrument sind zur Zeit keine Mitglieder eingetragen.
	</p>
<% end %>